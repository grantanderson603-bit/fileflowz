[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm build && pnpm db:push"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 3000
